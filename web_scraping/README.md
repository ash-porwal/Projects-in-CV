#Web Scrapping

Tree Structure down below

```
web_scraping/
├── config.ini #credential file
├── __int__.py #indicator that a directory should be treated as a Python package
├── README.md
├── requirement.txt #required packages
└── web_scraping.py #main file to fetch data

0 directories, 5 files
```

```
In this I've scrapped informaton from a website to list out books along with its price and buy link
```

```
All the needful packages are inside requirements.txt 

#To make virtual environment
python3 -m venv venv

#To activate source env
source venv/bin/activate

#To install pacakges from requirements.txt
python3 -m pip install -r requirements.txt

```
